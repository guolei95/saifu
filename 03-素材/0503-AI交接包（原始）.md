# 🤖 AI 交接包 — 竞赛选赛助手 V1 最新状态

> 📌 **阅读提示**：这是 0503 生成的状态快照文档。**如果你只想了解当前能不能跑、有什么问题 → 看本文档 §三、§五、§六。** 详细的架构/方案/调研/复盘内容已分发到 `02-方案/`、`01-调研/`、`05-复盘/` 文件夹。三份原始文档的关系见 `03-素材/00-阅读顺序说明.md`。
>
> ⚠️ **§六包含 API Key 明文，不要分享给他人！**
>
> 生成时间：2026-05-03  
> 给：项目管理 AI / 下一个接手的 AI  
> 代码位置：原 `D:\.claude\`（已迁移到本工作区 `02-方案/`）
> 原始文件：`D:\.claude\AI交接包_20260503.md`

---

## 一、本次会话改了什么

### 1. API 迁移：中转站 → DeepSeek 官方 ✅
**修改文件：** `match_competitions.py`
```python
# 旧（中转站 api-siyu.top）
RELAY_API_KEY = os.environ.get("RELAY_API_KEY", "")
RELAY_BASE_URL = "https://api-siyu.top/v1"
LLM_MODEL = "claude-opus-4-7"

# 新（DeepSeek 官方）
DEEPSEEK_API_KEY = os.environ.get("DEEPSEEK_API_KEY", "")
DEEPSEEK_BASE_URL = "https://api.deepseek.com/v1"
LLM_MODEL = "deepseek-chat"
```
**原因：** 中转站存在严重问题：
- 输出截断（JSON 被拦腰切断）
- 中文引号损坏（U+201C → U+0022，导致 JSON 断裂）
- DeepSeek 官方 API 100% 成功率，零错误

### 2. 修了两个 bat 文件
**修改文件：** `run_match.bat`
- 把 `set RELAY_API_KEY=` 改成 `set DEEPSEEK_API_KEY=`
- 提示文字相应修改

### 3. 多专业批量测试
**新增文件：**
- `match_from_cache.py` — 从缓存搜索 + DeepSeek 批量匹配的脚本
- `search_results_cache.json` — 6 个专业的搜索结果缓存
- `all_match_results.json` — DeepSeek 匹配原始数据
- `竞赛多专业测试报告.md` — 完整测试报告

### 4. ⚠️ Tavily API Key 过期
原 Key `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` 返回 Unauthorized。
本次测试用内置 MCP 搜索工具替代，6 个专业搜索结果已缓存到 `search_results_cache.json`。

---

## 二、当前文件清单

```
D:\.claude\
├── match_competitions.py          ← 主程序（已切 DeepSeek）
├── match_from_cache.py            ← 批量测试脚本（新增）
├── run_match.bat                  ← Windows 启动器（已切 DeepSeek）
├── requirements_competition.txt   ← Python 依赖
├── search_results_cache.json      ← 6 专业搜索缓存（新增）
├── all_match_results.json         ← DeepSeek 匹配结果（新增）
├── 竞赛信息平台_项目全记录.md      ← 项目文档（含完整踩坑记录）
├── 竞赛多专业测试报告.md           ← 批量测试报告（新增）
└── AI交接包_20260503.md           ← 本文件
```

---

## 三、当前可运行状态

### 主程序 `match_competitions.py`
- ✅ DeepSeek API 正常工作
- ❌ Tavily API Key 过期 → 搜索环节会报 Unauthorized
- **修复方法：** 换一个新的 Tavily API Key，改 `run_match.bat` 里 `TAVILY_API_KEY` 的值

### 批量测试脚本 `match_from_cache.py`
- ✅ 可正常运行（从缓存读搜索结果）
- 只处理缓存中有的 6 个专业

---

## 四、6 专业测试结论速览

| 专业 | 最佳匹配 | 分数 | 可报名 |
|------|---------|------|--------|
| 汉语言文学 | 汉语言文字能力大赛 | 95 | ✅ 6/28截止 |
| 法学 | 从法杯调研大赛 | 90 | ✅ 7/22开放 |
| 土木工程 | 数字建筑创新大赛 | 90 | ✅ 5/31截止 |
| 会计学 | 税收风险管控案例大赛 | 85 | 📅 秋季 |
| 经济学 | 能源经济学术创意大赛 | 85 | 📅 秋季 |
| 英语 | 外研社·国才杯 | 95 | 📅 10-11月 |

详细报告见：`竞赛多专业测试报告.md`

---

## 五、下一步行动（优先级排序）

### 🔴 高优先
1. **换 Tavily API Key** — 不换的话主程序 `match_competitions.py` 跑不了搜索
2. **修「截止日期未知」问题** — 60% 竞赛无截止日期，考虑加「预计报名时间」字段

### 🟡 中优先
3. 用 `match_from_cache.py` 再测 5-10 个专业（缓存新搜索结果即可）
4. 给 5-10 个同学看测试结果，收集真实反馈
5. 代码层做域名白名单（解决 sohu/知乎 被误标「官方」）

### 🟢 低优先
6. V2 网页 MVP：Flask 后端 + HTML 前端（一个路由 `/api/match`，一个表单）
7. URL 去重：同一比赛出现在两组的问题

---

## 六、API 配置（当前）

| 变量 | 当前值 | 状态 |
|------|--------|------|
| TAVILY_API_KEY | `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` | ❌ 过期 |
| DEEPSEEK_API_KEY | `sk-a78004ebddfb4b90b3a7603954ef43b1` | ✅ 有效 |
| DEEPSEEK_BASE_URL | `https://api.deepseek.com/v1` | ✅ |
| LLM_MODEL | `deepseek-chat` | ✅ |

---

## 七、开发日志新增（本次）

### 坑9：Tavily API Key 过期
- 现象：6 个搜索全部返回 Unauthorized
- 临时方案：用 MCP web_search 工具手动搜索 + 缓存结果
- 待修复：更换新 Tavily Key

### 坑10：中转站→DeepSeek 迁移
- 中转站不可修复的问题：输出截断 + 中文引号损坏
- DeepSeek 官方 API 完全正常，费用标注质量更好
- 切换只需改 3 个环境变量，代码逻辑不变

---

## 八、给下一个 AI 的三句话

1. **不要改架构**：搜索API + LLM匹配 这条路验证过了，能用
2. **先换 Tavily Key**：主程序就差这一个 Key 就能跑了
3. **费用标注问题已解决**：DeepSeek 能正确标出 300元/人、50元/人、49元/人

---

*原始文档保存时间：2026-05-03*
